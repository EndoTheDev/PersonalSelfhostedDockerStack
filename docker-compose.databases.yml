name: databases
services:
  redis:
    extends:
      file: services/databases/redis/docker-compose.yml
      service: redis
  qdrant:
    extends:
      file: services/databases/qdrant/docker-compose.yml
      service: qdrant

networks:
  services:
    external: true
